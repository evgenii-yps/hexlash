// Epic 3Ba — Training state reactive store.
// Step 1: stub. Real fields + reset + start + multiplierForCombo in Step 6.
//
// Pattern parity with Epic 3A useFightSimulation.

import { reactive } from 'vue';

export const trState = reactive({});

export function resetTrainingState() {}

export function startTrainingSession() {}

export function multiplierForCombo() { return 1; }
