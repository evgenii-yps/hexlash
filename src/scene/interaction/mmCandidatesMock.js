// Epic 3Bb — Matchmaking mock candidate generator.
// Step 1: stub. Real pools + seeded RNG + generateCandidates in Step 8.
// Source target: prototype 10531-10613.

export const MM_POOL_NAMES = [];
export const MM_ARCHS = [];
export const MM_BELTS = [];

export function generateCandidates(/* mmState */) {
  return [];
}
