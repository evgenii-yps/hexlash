// Epic 3Ba — Training click-to-hit handler.
// Step 1: stub. Raycaster + impulse + energy in Step 7a.
// Combo + tasks + tap-pop + particles in Step 7b.
// Hit sound wiring in Step 9.

export function attachClickToHit(/* THREE, camera, bag, applyImpulse, onEnergyEmpty, spawnHitParticles */) {
  return {
    detach: () => {},
  };
}
