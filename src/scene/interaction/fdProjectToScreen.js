// Epic 3A — project 3D world position to screen pixels.
// Step 1: stub. Real implementation in Step 8 (branch label tracking).

export function fdProjectToScreen(obj3d, addY, camera, THREE) {
  return { x: 0, y: 0, visible: false };
}
