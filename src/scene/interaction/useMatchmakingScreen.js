// Epic 3Bb — Matchmaking CRT screen renderer + typeLog animation.
// Step 1: stub. refreshScreen canvas render + startSearchLogAnimation in Step 5.

export function refreshScreen(/* ctx, tex */) {}

export function startSearchLogAnimation(/* ctx, tex, onComplete */) {
  return { cancel: () => {} };
}
