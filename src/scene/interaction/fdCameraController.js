// Epic 3A — Fighter Detail drag-to-rotate camera.
// Step 1: stub. Real implementation in Step 6.

export function attachFdOrbit(camera, canvas) {
  return {
    tick() {},
    detach() {},
    getIsDragging() { return false; },
  };
}
