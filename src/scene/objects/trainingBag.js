// Epic 3Ba — Training heavy bag (central, visible 6-link chain).
// Step 1: stub. Real geometry in Step 4.
// NOT the hub heavyBag.js (smaller, side-position) — own module per ТЗ 2.2 §4.

export function buildTrainingBag(/* THREE */) {
  return null;
}
