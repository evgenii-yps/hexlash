// Epic 3Bb — Matchmaking terminal (stand + CRT body + canvas screen).
// Step 1: stub. Real geometry + CRT canvas in Step 4.
// NOT the hub `terminal.js` — different size/position/screen pipeline.
// Pattern 3Ba: one object = one module.

export function buildMatchmakingTerminal(/* THREE */) {
  return null;
}
