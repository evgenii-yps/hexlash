// Epic 3Bc Step 1 — Archetype glow stub.
// Full implementation Step 6 (canvas gradient texture + PlaneGeometry disc +
// PointLight rebuild on setColor). Prototype 8952-8983.

export function createArchetypeGlow(/* THREE, podium */) {
  return {
    setColor(/* hex */) {},
    dispose() {},
  };
}
