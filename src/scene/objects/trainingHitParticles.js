// Epic 3Ba — Training hit particles (additive sparks on impact).
// Step 1: stub. Real factory in Step 7b.

export function createHitParticles(/* scene, THREE */) {
  return {
    spawn: () => {},
    tick: () => {},
    dispose: () => {},
  };
}
