// Epic 3Ba — Training scene.
// Step 1: stub. Populated across Steps 2-9 (scaffold, lighting+dust, bag,
// physics, HUD binding, click-to-hit, particles, sound).

export function buildTrainingScene(/* THREE, aspect */) {
  return {
    scene: null,
    camera: null,
    tick: () => {},
    dispose: () => {},
  };
}
