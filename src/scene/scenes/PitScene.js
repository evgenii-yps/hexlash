// Epic 2 — pit-view hub. Step 1 (STUB). See docs/visual-migration/PROMPT_EPIC2_FOR_CLAUDE_CODE.md
// Реальная реализация — Шаг 3. Пока возвращаем null-scene, чтобы prod-билд не падал.

export function buildPitScene() {
  return { scene: null, camera: null, tick: () => {}, clickableTargets: [] };
}
