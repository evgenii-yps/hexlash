// Epic 3Bb — Matchmaking scene.
// Step 1: stub. Populated across Steps 2-5 (scaffold, lighting+dust,
// terminal, screen texture).

export function buildMatchmakingScene(/* THREE, aspect */) {
  return {
    scene: null,
    camera: null,
    tick: () => {},
    dispose: () => {},
  };
}
