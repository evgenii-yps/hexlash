// Epic 3A — Fighter Detail scene.
// Step 1: stub. Will be populated across Steps 2-7 (scaffold, lighting,
// fighter, branch columns, orbit camera, picker).

export function buildFighterDetailScene(THREE, aspect) {
  // TODO(step 2+): populate.
  return null;
}
