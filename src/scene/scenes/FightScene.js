// Epic 3A — Fight scene.
// Step 1: stub. Populated across Steps 9-13 (scaffold, lighting, fighters,
// animation system, camera modes).

export function buildFightScene(THREE, aspect) {
  // TODO(step 9+): populate.
  return null;
}
