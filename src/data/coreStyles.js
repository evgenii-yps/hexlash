/* HEXLASH — ВАРИАНТЫ ОФОРМЛЕНИЯ ЯДРА (прототип, ТЗ 21.09.2026 правка 2).
   Только для скрытой страницы /dev/core. В продукт ничего из этого пока не
   подключено.

   Каркас у всех вариантов ОДИН и задан в src/components/core/HexCore.vue:
   шестиугольник вершиной вверх · центр, яркость которого растёт с числом
   задействованных веток · три ветки к трём вершинам · пять граней на ветке ·
   свет от центра до последней горящей грани · сплав в зоне между двумя
   задействованными ветками · цвет ядра приходит из src/styles/tokens.css.

   Здесь живёт ТОЛЬКО оформление этого каркаса — то, чем варианты отличаются.
   Данные (ветки, грани, лимит) читаются из src/data/upgradeData.js и у всех
   вариантов одни.

   ⚠️ Новых цветов здесь нет и быть не может: только геометрия и доли. */

/* Длины и радиусы — в единицах бокса 200×200, внешний радиус фигуры R = 86. */

export const CORE_STYLES = [
  {
    id: 'origin',
    name: 'ИСХОДНЫЙ',
    idea: 'Ровная схема из правки 1: одинаковые каналы, ромбы-грани, рамка.',
  },
  {
    id: 'crystal',
    name: 'ЖИВОЙ КРИСТАЛЛ',
    idea: 'Ветки — кристаллы, растущие из ядра: толстые у центра, сужаются к краю. Плоских граней куба нет, тело — свет изнутри.',
  },
  {
    id: 'heart',
    name: 'СЕРДЦЕ КРУПНЕЕ',
    idea: 'Центр втрое крупнее и главный в фигуре, ветки короче и тоньше — сразу видно ядро с тремя отростками.',
  },
  {
    id: 'frameless',
    name: 'БЕЗ РАМКИ',
    idea: 'Внешней линии нет: силуэт складывают три пластины камня с щелями, из щелей бьёт свет ядра.',
  },
  {
    id: 'sigil',
    name: 'ЗНАК МАНЕРЫ',
    idea: 'Крупное сердце, а внутри светится знак манеры своего ядра — различать можно без цвета.',
  },
];

export const CORE_STYLE_IDS = CORE_STYLES.map((s) => s.id);

/* Поля настройки. Отсутствующее наследуется от origin (см. resolveStyle). */
const BASE = {
  /* Сердцевина */
  heartR: 17,        // радиус огранённого камня в центре
  heartSeed: 7.5,    // самая яркая точка внутри
  heartGlow: 46,     // радиус ореола
  /* Огранка сердца:
     'cuts'  — три ребра к вершинам (годится мелкому сердцу);
     'table' — площадка гранёного камня: внутренний шестиугольник и шесть
               коротких рёбер. Крупному сердцу нужна именно она: три ребра к
               вершинам продолжают ветки и превращают камень в кубик, а вместе
               с внешним шестиугольником выходит куб внутри куба;
     'none'  — ничего (место занимает знак манеры). */
  heartFacets: 'cuts',
  sigil: false,      // знак манеры в сердце (рисунок берётся из upgradeGeometry)

  /* Ветка */
  tipFrac: 0.935,    // докуда доходит канал, доля R
  channel: 'groove', // 'groove' — паз ровной ширины · 'taper' — сужающийся кристалл
  chanW0: 10.5,      // полуширина у центра
  chanW1: 10.5,      // полуширина у вершины
  flowW: 13,         // толщина светящейся жилы
  walls: true,       // подсвеченные стенки паза

  /* Грани */
  nodeT: [0.275, 0.43, 0.585, 0.745, 0.915],
  nodeR: [3.0, 3.7, 4.5, 5.4, 7.0],
  nodeLong: 1.12,    // вытянутость кристалла вдоль луча
  nodeRough: 0,      // неровность огранки: 0 — правильный ромб, 1 — заметно кривой
  tipRsimple: 11,    // радиус вершины в упрощённой отрисовке (ниже 48 точек)

  /* Тело и рамка */
  zones: 'flat',     // 'flat' — три плоские грани · 'soft' — мягкая подложка · 'faint' — почти нет · 'plates' — пластины со щелями
  plateInset: 0,     // отступ пластины от лучей и краёв (для 'plates')
  rim: true,         // внешний контур-шестиугольник линией
};

const OVERRIDES = {
  origin: {},

  /* А · Живой кристалл. Ветка толстая у ядра и сходит на нет к краю; грани —
     наросты, растущие к вершине; плоских граней куба нет — тело светится
     изнутри, от чего фигура перестаёт читаться коробкой. */
  crystal: {
    channel: 'taper',
    chanW0: 15,
    chanW1: 3.4,
    flowW: 11,
    walls: false,
    tipFrac: 0.96,
    nodeT: [0.30, 0.45, 0.60, 0.755, 0.905],
    nodeR: [2.6, 3.4, 4.6, 6.2, 8.6],
    nodeLong: 1.45,
    nodeRough: 0.34,
    zones: 'soft',
    heartR: 20,
    heartSeed: 8.5,
    heartGlow: 54,
  },

  /* Б · Сердце крупнее. Камень в центре главный: втрое больше исходного,
     ветки короче и тоньше и явно прикреплены к нему. */
  heart: {
    heartR: 34,
    heartSeed: 15,
    heartGlow: 62,
    heartFacets: 'table',
    tipFrac: 0.90,
    channel: 'taper',
    chanW0: 8.5,
    chanW1: 4,
    flowW: 7,
    walls: false,
    nodeT: [0.50, 0.605, 0.705, 0.805, 0.905],
    nodeR: [2.4, 2.9, 3.4, 4.0, 5.4],
    nodeLong: 1.2,
    nodeRough: 0.18,
    zones: 'faint',
    tipRsimple: 9,
  },

  /* В · Без рамки. Внешней линии нет — силуэт собирают три пластины камня,
     отодвинутые друг от друга; в щелях между ними виден свет ядра. */
  frameless: {
    rim: false,
    zones: 'plates',
    plateInset: 7,
    channel: 'taper',
    chanW0: 12,
    chanW1: 4.5,
    flowW: 10,
    walls: false,
    tipFrac: 0.95,
    nodeT: [0.30, 0.45, 0.60, 0.755, 0.905],
    nodeR: [2.8, 3.5, 4.4, 5.6, 8.0],
    nodeLong: 1.3,
    nodeRough: 0.22,
    heartR: 19,
    heartSeed: 8,
    heartGlow: 58,
  },

  /* Г · Знак манеры. Основа — крупное сердце, в нём светится знак своего
     ядра. Рисунки знаков берутся из нынешней иконки (upgradeGeometry),
     второй копии координат не заводим. */
  sigil: {
    heartR: 36,
    heartSeed: 0,       // семечка не нужна — её место занимает знак
    heartGlow: 64,
    heartFacets: 'none',
    sigil: true,
    tipFrac: 0.90,
    channel: 'taper',
    chanW0: 8,
    chanW1: 3.6,
    flowW: 6.5,
    walls: false,
    nodeT: [0.525, 0.62, 0.715, 0.81, 0.905],
    nodeR: [2.3, 2.8, 3.3, 3.9, 5.2],
    nodeLong: 1.2,
    nodeRough: 0.16,
    zones: 'faint',
    tipRsimple: 9,
  },
};

/** Настройки варианта. Неизвестный id падает на исходный. */
export function coreStyleCfg(id) {
  return { ...BASE, ...(OVERRIDES[id] || OVERRIDES.origin) };
}

/** Описание варианта для подписи на превью. */
export function coreStyleInfo(id) {
  return CORE_STYLES.find((s) => s.id === id) || CORE_STYLES[0];
}
